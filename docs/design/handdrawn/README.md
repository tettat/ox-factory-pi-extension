# 牛马手绘工作室：保存稿与动画小样

2026-09-08。基于 main@afec2e7，分支 azhe/handdrawn-design。

## 已保存
- `compaction-concept.png`：用户认可的手绘压缩页设计方向，示例数据，不是线上截图。
- `running-mascots.svg` / `resting-mascots.svg`：新绘制的矢量牛马小样，不是生成图中角色的精确描摹。CSS 仅动画 transform/opacity，无 JS 帧循环、网络请求或依赖。
- `preview.html`：独立预览，可暂停、切换现有配色参考/手绘配色；尊重 reduced-motion。这里不是现有生产组件的皮肤实现。
- `layout.css` 管几何结构，`skins.css` 管颜色/材质；偏好 key 使用 ox-design-preview-skin，不影响现有 Web 设置。

可直接打开 HTML；或在此目录 `python3 -m http.server 8794 --bind 127.0.0.1`。

## 实施约束（已经对齐，尚未接入生产）
1. 排版独立提交：筛选、指标、图表、详情抽屉的 DOM 与交互，不改变默认皮肤。
2. 皮肤独立提交：classic 保留并作为默认，handdrawn 可选，统一 CSS tokens；同一 DOM，不复制两套页面。
3. 手绘仅影响背景、边框、图标与局部标题；正文及数字用常规字体，状态不能只依赖颜色。
4. 动画独立组件：仅真实 running 状态播放，idle/paused 静止、failed 静止并显示错误。不能用跑步掩盖服务掉线。
5. 大列表不逐卡播放，优先只在总状态或当前员工详情放一组；不可见/后台页面暂停，遵守 reduced-motion。
6. 不用整页 PNG 实现皮肤，不引入重型动画库。纸纹用小资源或 CSS；性能排查和接口分页另行实施。

## 验证
JS 语法检查、两份 SVG XML 解析通过。当前为独立小样，尚未证明跨浏览器动画兼容性，不宣称已实现生产主题切换。

## 角色动作 V2
- 统一角色稿：圆框眼镜、围巾、四肢蹄部、牛角与耳内轮廓、马鬃层次、腮红、眨眼。两只角色共享同一套动作定义，避免不同动作换了形象。
- 轻快跑步：0.72s 步态周期；快跑：0.34s，增加前倾、腾起与扬尘；吃草：6s 低头/抬头周期，0.45s 咀嚼。
- 所有动作提供各自静止 SVG，暂停或 reduced-motion 不会把吃草强行切回跑步造型。
- `build-mascots.py` 为 SVG 源码生成器，Python 标准库即可运行；`python3 docs/design/handdrawn/build-mascots.py` 更新全部 SVG。未引入运行时生成依赖。
- `node --test docs/design/handdrawn/preview.test.mjs`：4 项预览逻辑测试通过；7 个 SVG XML 解析与无脚本/外链检查通过，预览及快跑/吃草 HTTP 200。
- 当前工具环境没有可用浏览器，尚未做实际浏览器动效目测或跨浏览器测试。这里仍是独立设计小样，不是生产组件。

## 小剧情与新角色提案
新增 `river-leopard-storyboard.png`（概念图，不是 SVG 成品动画）。
- “豹拉”暂按新伙伴小豹子的名字理解：赭金毛色、花斑、鼠尾草绿围巾、帆布小包；轻巧、有点得意，但会等伙伴。
- 结伴过河：探路 → 豹拉先跃过 → 牛马跟上 → 全员会合。适合独立演示或真实完成后的短庆祝，不应在任务仍失败时假装通关。
- 可继续扩展：推草车/捡落草、共撑小伞、下班打盹、看地图等伙伴、击蹄庆祝。
- 页面运行状态仍使用低干扰短循环。叙事动画作为可点开的彩蛋或一次性反馈，不永久循环占据统计页。
- 用户提到的“电影牛”具体片名待确认，当前没有声称按某一电影角色制作。

## 豹拉与一车牛 SVG（V3）
- `build-companions.py` 引用原牛马生成器，沿用轮廓色与动效定义；不把插画 PNG 嵌入 SVG。
- 新增豹拉站立张望、快跑、蓄力跳小河三种 SVG。赭金底色、玫瑰斑、圆耳、白口鼻、长尾和绿色围巾，与牛马同系列。
- 新增 `cow-truck-mascots.svg`：同一套小牛素材，车上乘客与错时下车循环。不是整个车队真实调度状态。
- 每种新增动作都有对应静止版本，动作选择与皮肤选择独立。小河动效循环时淡出重置，不声称物理模拟。
- 预览默认展示豹拉。运行 `python3 docs/design/handdrawn/build-companions.py` 可重建全部角色及场景 SVG。
- 检查：预览逻辑测试4项通过（动作资产断言覆盖全部7种）；15份SVG XML解析通过；未接入生产页面，尚未完成浏览器视觉验收。

## 紧凑侧门版面包车（V4）
- 车体改为三分之四透视，前脸斜向屏幕，中部侧滑门保持打开；去除横向长坡道。
- 小牛重绘为正面眼镜/围巾造型，出门后沿画面下方前进、逐渐放大，而不是横向排长队。
- 独立画布 400×360（此前 640×240），预览最大显示宽度400px；车与牛主要沿纵深方向展开。
- 无可见文字、无车顶牛的要求保持不变。暂停/减少动态效果仍有对应静止队列；豹拉素材未更改。
- 6项预览测试通过；SVG XML通过；HTTP返回内容已确认包含侧门与新画布。仍待实际浏览器目测确认透视与步态。
