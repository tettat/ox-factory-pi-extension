# 员工列表：任务未读与执行状态

- 分支基点 main@269e2ca；只改变员工列表口径，原始消息/event 未读字段保留兼容。
- 红点按已结束 Job 数聚合，done/failed/aborted/stale 均算；流式事件不重复计数。
- 正在执行显示“正在输入…”，排队显示“等待执行…”，无任务/未读显示“空闲”。旧任务未读可与新任务执行状态并存。
- 已结束未读优先，其次运行中，组内最近交互优先。每 5 秒仅刷新可见员工列表，不重建输入框，独立于通知开关。
- 沿用打开员工详情批量标记已读、打开 Job 标记对应 Job 已读的交互。运行中已读不吞掉后续完成提示。
- 当前“任务”单位为 Job，不跨多个 Job 合并成看板任务或对话线程。

## 验证

新增 test/task-unread.test.mjs：事件聚合、运行转完成、终态/排队、API 输出与已读、UI 状态文案、排序。
pnpm run check 通过；补齐主 worktree 的本地 ignored 文档后，pnpm test 通过。未 reload，未作真实浏览器视觉验收。
